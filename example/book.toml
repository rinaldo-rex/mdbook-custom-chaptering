[book]
title = "My CalVer Book"
authors = ["Your Name"]
description = "A book with custom CalVer chapter numbering"
src = "src"

[build]
create-missing = false

[preprocessor.custom-chaptering]
# Enable or disable custom chapter numbering (default: true)
enabled = true

# Run only with HTML renderer
renderers = ["html"]